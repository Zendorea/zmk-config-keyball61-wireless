# Shared config for both halves.
# (Trackball pointing is enabled by the tangbonze zmk,pmw3610 driver's own
#  Kconfig on the right half; nice!view display is enabled on the left half only.
#  Do NOT put CONFIG_ZMK_DISPLAY here — the right half has no display node.)
